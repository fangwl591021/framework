# Canary Plan

Canary plans require explicit Tenant and Application allowlists, bounded percentage, request and estimated-cost caps, observation window, rollback thresholds, and an owner. Wildcards are forbidden.

This release stores `draft` only. It cannot activate Canary traffic, add a Tenant, or grant Production authority.
