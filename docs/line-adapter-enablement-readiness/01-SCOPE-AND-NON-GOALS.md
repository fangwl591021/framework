# Scope and Non-Goals

Status: Lifecycle **Readiness Candidate**; Real LINE Adapter **Disabled**; Credentials **Not Provisioned**; Remote D1 **Not Used**; Deployment **Not Performed**; Production Use **Not Allowed**.

The scope is pure provider-specific contracts, deterministic fixtures, validation helpers, readiness decisions, safe evidence types, documentation, and local tests. It introduces no handler, route, SDK, transport, secret loading, binding, schema, outbound request, or production composition.

The existing Channel Adapter owns authenticity, normalization, replay coordination, trusted identity resolution, rendering, and evidence. The Conversational Workbench remains the only intent, confirmation, permission, module, and mutation authority.
