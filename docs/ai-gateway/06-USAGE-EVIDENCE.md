# Usage Evidence

每個完成的請求最多一筆 immutable Usage Record，記錄 Task／Route Version、units、estimated cost、cache outcome、latency 與 bounded failure code。

不得保存完整 input、prompt、response、Secret、Token、raw UID、Request Body、Stack 或 SQL。Usage 是治理證據，不是發票或正式 Billing Ledger。
