# Workbench Integration

預設 `deterministic_only`；`gateway_shadow` 僅供 Local Test；`gateway_enabled_future` 保持 disabled。Shadow 結果不得取代既有 authorization、slot validation、confirmation、idempotency 或 operation router。

AI 只提供候選 intent／clarification，不是執行 authority。
