# Known Limitations

- 無真實 AI Provider、SDK、Credential、Binding 或 Remote call。
- Workbench production path 維持 deterministic；AI-enabled mode 尚未核准。
- Cost 僅是 estimate，不是 Billing。
- 無 streaming、embedding、knowledge retrieval、fine tuning、image/audio。
- 無 production coordination、scheduler、deployment 或 provider compliance verification。
