# Local Verification

Fresh Isolated Local D1 只依正式 0001～0008 Migration。驗證 Schema／FK／Index／Trigger、Permission Gate forced rollback、budget winner、idempotency conflict、cache accounting、fallback、output rejection、Tenant Isolation、Usage immutability、Workbench mode、health／ready regression。

Local verification 不代表 Remote／Production Migration、Provider Verification 或 Production Readiness。
