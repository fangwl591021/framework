# Scope and Non-Goals

Status: Lifecycle **Isolated Verification Candidate**; Real LINE Adapter **Disabled**; Provider Transport **Fake Only**; Credentials **Not Provisioned**; Public Webhook **Not Created**; Remote D1 **Not Used**; Deployment **Not Performed**; Production Use **Not Allowed**.

The scope is pure TypeScript contracts, published and synthetic deterministic fixtures, byte-preserving verification, bounded normalization, in-memory replay and reply-token models, fake transport outcomes, safe evidence, and local tests. No package export is added to the production entry or Local Demo composition.

There is no SDK, provider call, route, secret loader, binding, schema, queue, scheduler, Remote D1, deployment, or production approval. The Channel Adapter boundary remains unchanged, and the Conversational Workbench remains the only intent, confirmation, permission, and mutation authority.
