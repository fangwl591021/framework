# Runtime Phase 1 Physical Proposal Gap

> Supersession note: this pre-implementation gap assessment is retained as history. The Phase 1-only reconciliation and Local D1 evidence are in [Core Persistence and Domain Foundation Status](09-CORE-PERSISTENCE-DOMAIN-STATUS.md).

> Documentation-only gap record. No SQL or Migration is changed or authorized.

## Why This Gap Exists

The existing RC1 Physical D1 Schema Proposal predates the Runtime Phase 1 Decision Closure. It remains an architecture-reviewed, unexecuted proposal for a broader Framework scope and must not be copied directly into the Phase 1 Migration Package.

## Required Future Reconciliation

| Existing Proposal | Phase 1 Contract | Required Future Action |
| --- | --- | --- |
| Opaque `TEXT` ID; generator pending | UUIDv7 generated by Application／Domain Service | Update the future approved Migration Proposal after ADR-013 acceptance |
| `provider_subject_hash`／`provider_context` | `subject_digest`／`issuer_context`／`digest_key_version` with HMAC-SHA-256 | Security review canonicalization and rotation; propose new columns only in a later SQL PR |
| Storage role scope `core_template`／`tenant_defined` | Logical `scope_type` is `core` or `tenant` | Define explicit storage mapping; neither vocabulary may be silently inferred |
| Organization includes Brand／Shop／Shop Membership | Phase 1 excludes all three | Extract a reviewed Phase 1 subset; do not execute `001-core-identity-membership.sql` wholesale |
| Existing Audit／Idempotency candidate fields | Core Operations requires Actor types, safe Stored Result and stale-owner fencing | Reconcile physical fields after Security and Migration Gates |

## Gate

```text
Physical Reconciliation: Required
SQL Change: Not Authorized
Migration: Not Authorized
D1 Execution: Not Authorized
Verification: Not Verified
```

The Decision Closure architecture is approved without resolving physical storage. Runtime persistence and Migration remain NO-GO until a separate Proposal reconciles every row above and passes its own Architecture, Security, Test Plan and Execution Gates.
